{
  "name": "flightdeck-for-jekyll-with-parcel",
  "version": "0.1.0",
  "description": "Flightdeck is an opinionated starter project for Jekyll that uses Parcel.",
  "author": "Ed Heltzel <ed@rainyday.media>",
  "license": "MIT",
  "homepage": "",
  "scripts": {
    "preinstall": "bundle install --path vendor/bundle",
    "start": "run-p jekyll watch:parcel bs:delay",
    "jekyll": "bundle exec jekyll build --watch",
    "watch:parcel": "run-p watch:css watch:js watch:images",
    "watch:css": "parcel watch assets/css/app.scss --out-dir _site/assets/css",
    "watch:js": "parcel watch assets/js/bundle.js --out-dir _site/assets/js",
    "watch:images": "parcel watch assets/images/* --out-dir _site/assets/images",
    "build": "run-s clean prod:*",
    "prod:jekyll": "bundle exec jekyll build JEKYLL_ENV=production",
    "prod:parcel": "run-p build:css build:js build:images build:postcss",
    "build:css": "parcel build assets/css/app.scss --out-dir _site/assets/css",
    "build:postcss": "npx postcss _site/assets/css/app.css --use autoprefixer -d .",
    "build:js": "parcel build assets/js/bundle.js --out-dir _site/assets/js",
    "build:images": "parcel build assets/images/* --out-dir _site/assets/images",
    "bs:start": "browser-sync start --config .bs-config.js",
    "bs:delay": "run-s delay:message bs:start",
    "svg": "svg-sprite-generate -d assets/svgs -o assets/icons",
    "delay:message": "echo '\u001b[33mBrowsersync will launch after Assets finish...\u001b[0m'",
    "deploy:test": "./.liftoffrc ready",
    "deploy": "./.liftoffrc ready go",
    "clean": "npm run clean:ruby & npm run clean:node",
    "clean:ruby": "rm -rf vendor/ .bundle/ Gemfile.lock",
    "clean:node": "rm -rf node_modules yarn.lock package-lock.json pnpm-lock.yaml .cache/",
    "clean:site": "rm -rf _site/ .jekyll-cache && bundle exec jekyll clean",
    "clean:all": "npm run clean:ruby & npm run clean:node & npm run clean:site",
    "purge": "npm run clean:all"
  },
  "devDependencies": {
    "@babel/core": "^7.12.3",
    "babel": "^6.23.0",
    "babel-preset-env": "^1.7.0",
    "browser-sync": "^2.24.6",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "parcel-plugin-imagemin": "^4.0.1",
    "prettier": "^2.1.2",
    "sass": "^1.29.0",
    "stylelint": "^13.7.2",
    "stylelint-prettier": "^1.1.2",
    "svg-sprite-generator": "^0.0.7"
  },
  "dependencies": {}
}
